#include <stdio.h>

int main() {
    int marks = 95;        // 'marks' is an identifier AND a variable
    float average = 89.5;  // 'average' is an identifier AND a variable

    printf("Marks = %d\n", marks);
    printf("Average = %.2f\n", average);

    return 0;
}

