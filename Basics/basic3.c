#include<stdio.h>
void main()
{
    float celsius, fahrenheit;
    scanf("%f",&celsius);
    fahrenheit = ((celsius * 1.8) + 32);
    printf("Fahrenheit = %.2f\n",fahrenheit);

    printf("Sum = %d",a+b);
    int sum;
    sum = a+b;
    printf("Sum = %d",sum);
}
